# TEST environment overrides for Crunchy Postgres cluster.
# High-availability: 2 instances + 2 pgBouncer replicas, separate S3 backup path.

crunchy-postgres:
  fullnameOverride: biohub-platform-db-test

  instances:
    replicas: 2
    dataVolumeClaimSpec:
      storage: 10Gi
    requests:
      cpu: 50m
      memory: 256Mi

  proxy:
    pgBouncer:
      replicas: 2

  pgBackRest:
    retention: "14"
    s3:
      s3Path: "/biohub/test"

  patroni:
    postgresql:
      parameters:
        shared_buffers: 32MB

# To perform the one-time data migration from the old database, set:
#   migration.enabled: true
#   migration.oldDb.host: "biohub-platform-db-test"
#   migration.oldDb.secretName: "biohub-platform-db-postgresql-test"
# Then re-deploy with migration.enabled: false once complete.
migration:
  enabled: false
  oldDb:
    host: "biohub-platform-db-test"
    secretName: "biohub-platform-db-postgresql-test"