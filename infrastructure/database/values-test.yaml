# Test environment values for Biohub
environment:
  name: test
  id: deploy

# Database configuration
app:
  nodeEnv: production

# Openshift Resources
replicas: 1
resources:
  requests:
    cpu: 50m
    memory: 100Mi
  limits:
    cpu: 1500m
    memory: 5Gi

# Volume for persistent data
persistence:
  size: 3Gi