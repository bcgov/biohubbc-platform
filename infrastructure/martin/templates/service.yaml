apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.fullname" . }}
  namespace: {{ include "environment.namespace" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
  annotations:
    {{- include "app.annotations" . | nindent 4 }}
spec:
  ports:
    - name: {{ .Values.app.name }}-{{ .Values.service.name }}
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    deployment: {{ include "app.fullname" . | quote }}
  sessionAffinity: None
  # ClusterIP only. Martin is never exposed through an OpenShift Route - tile requests are
  # authenticated by the Martin Gateway (SIMSBIOHUB-1102), which is the only public entry point.
  type: {{ .Values.service.type }}
